.env

# macOS metadata
.DS_Store
**/.DS_Store

# Keep any local IB Client Portal Gateway install out of git.
# Recommended location is outside the repo, but this also protects
# against accidentally unpacking it in the project root again.
clientportal.gw/
